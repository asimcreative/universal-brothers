# Regression Test Results

This is a log of actual executed test runs across this engagement, not a plan. Every number below comes from a real `php artisan test` or `npx playwright test` invocation in this session.

## Current state (latest run)

- **PHPUnit: 145/145 passing, 516 assertions.** Package/business data integrity directly re-verified: 12 Hajj packages, 47 total packages, 67 room options, 7 awards, 10 affiliations, 0 orphaned test records, 0 real inquiries (a local dev DB — no real customer data ever existed in it).
- **Playwright: 5-project suite run multiple times through Run 21's fix cycle, alone (not concurrently — see Run 21), reaching 0 unresolved failures on the final run.** One run had a test fail on both its attempt and its `retries: 1` retry — re-ran that exact test in isolation immediately after (1/1 clean) and the full suite again right behind it (0 unresolved failures), confirming a one-off unlucky double-occurrence of the same pre-existing Run 17 flake rather than a new regression. Consistent with the established diagnosis: genuine environment-load noise on this local machine over a long session, never reproducing in isolation, not a deterministic defect.

## Full regression history this session (chronological)

| Run | PHPUnit | Playwright | What changed since last run |
|---|---|---|---|
| 1 | 33/33 | — | Initial CMS + public site build |
| 2 | 41/41 | — | Added Pages/About Us module |
| 3 | 41/41 | 9 failed (admin) | First Playwright run — caught: unassociated `<label>`s across 12 views blocking `getByLabel()` (real accessibility defect, not just a test issue), no `login` named route causing a 500 on unauth admin access |
| 4 | 41/41 | 4 failed (public) | Fixed labels; caught: Blade's built-in `@context` directive silently corrupting a literal JSON-LD `"@context"` key, breaking every page |
| 5 | 41/41 | 1 failed (contact) | Fixed `@context` escaping; caught: HTML5 `required` blocking empty-submit before it ever reaches the server (test design fix, not an app bug) |
| 6 | 41/41 | 17/17 public spec clean | — |
| 7 | 41/41 | 9 failed (all admin, same cause) | Admin login form had the same unassociated-label bug as the other 11 views — fixed |
| 8 | 51/51 | 4 failed | Root-caused: Laravel's bare `throttle:5,1` shares ONE bucket per IP across unrelated routes (contact/inquiry/login) — real production bug, fixed with named `RateLimiter::for()` limiters; also caught a real `TypeError` in `PackageController::syncNestedData()` and NOT-NULL `sort_order` violations in 4 controllers (Faq/Office/Slider/Testimonial) — Office's admin form had **no** sort_order field, so creating an Office via the real UI failed on every submission until this fix |
| 9 | 52/52 | 46/50 (4 skipped) | Fixed a strict-mode selector ambiguity and a genuine cross-project rate-limit contention issue (restructured mobile project to stop re-testing business logic already proven on desktop); fixed a real N+1 query bug (homepage 58→23 queries) and a missing-from-sitemap gap for the Pages module — both caught by measurement, not assumption |
| 10 | 65/65 | 49/53 (4 skipped) | Closed the Media Gallery admin module (was schema-only) — controller, views, route explicitly pinned to `->parameters(['media' => 'item'])` to pre-empt a repeat of the News implicit-binding bug, 5 PHPUnit tests, 1 Playwright image-upload CRUD test |
| 11 | 84/84 | 50/54 (4 skipped) | Independent Senior-Team-Lead code review (FINAL_CODE_REVIEW.md) found 1 CRITICAL (stale by completion), 4 HIGH, 9 MEDIUM/LOW/INFO findings. Fixed and regression-tested: Settings cache never invalidating (raw mass-update bypassed Eloquent's `saved` event — caught a *second* bug fixing this one, since the blank-secret check needed `blank()` not `=== ''` because `ConvertEmptyStringsToNull` turns a blank field into `null`, not `''`), no public News article page, the entire Users & Roles module + policy-based authorization missing, soft-deleted package slug/code blocking reuse (required both a scoped validation rule *and* a migration dropping a raw DB-level UNIQUE constraint neither MySQL nor SQLite can portably scope to non-trashed rows), plaintext secret settings, a third redundant Office/nav query, a hardcoded footer Services list, dead footer legal links (closed with honest placeholder Pages, not invented legal text), hardcoded homepage stat counters, and a nullable-`Str::limit` fragility repeated in 3 views. While proving the fix via 2 consecutive full Playwright runs, found and fixed a separate real bug: 3 pre-existing E2E tests permanently overwrote real seeded content (a testimonial quote, package UB001's real SEO meta fields, the About Us page body) with no cleanup, and 2 more used non-unique fixture text — all 5 fixed, and the corrupted real dev data (plus a duplicate row one of the fixes' own idempotent seeder produced) was restored/removed. |
| 12 | 97/97 | 53/57 (4 skipped) | Full Hajj package data model redesign after the client flagged the previous structure as "too generic" — re-read the entire 37-page brochure page by page (found real, previously-missed structure: Package A/B prices vary by variant per location independently, a 4th sharing type not in any fixed enum, different Aziziya-vs-non-Aziziya Kaba-view-supplement values, a real TOC error for UB013). Added 11 new tables, a dedicated Hajj admin controller/form, a dedicated public Hajj view with a client-side currency switcher, and fully rewrote the seeder — caught and fixed an `Undefined array key` bug in the new admin controller from the same "missing key vs. blank" class of bug this project has hit before (`?:` needs the key to exist; switched to `??`), a table-name pluralization mismatch (`PackageTransportation` defaulting to `package_transportations`), and a nav-text ambiguity in an existing Playwright test once "Hajj Packages" was added next to "Packages" in the sidebar. The new admin E2E test (test 28) edits real seeded package UB001 through the live repeater form and explicitly restores it afterward — verified by direct query after 2 full Playwright runs that UB001's real room-option count returned to exactly 6 with no leftover test rows, continuing this session's established practice of never leaving a test's own fixture data behind on real content. Cross-checked all 67 seeded room-option prices against the brochure directly — zero discrepancies (see HAJJ_PACKAGE_DATA_AUDIT.md). |

| 13 | 102/102 | 53/57 (4 skipped) | Independent Senior-Team-Lead code review scoped specifically to the Hajj redesign (FINAL_CODE_REVIEW_HAJJ_REDESIGN.md) found 2 CRITICAL findings this session's own work had missed: `HajjPackageController::syncNestedData()` ran its 10-table delete-then-recreate sync with no `DB::transaction()` and `HajjPackageRequest` validated none of the ten nested repeater arrays — together meaning a single bad row (two variant rows submitted with the same code was the easiest way to trigger it) could cascade-delete a live package's real pricing data via the new `variant_id` foreign keys, then crash with an unhandled 500 before any replacement was created. A second, independent CRITICAL: the Media repeater's delete-then-recreate logic would silently delete every previously-uploaded photo on the very next unrelated edit, since a file input can never be pre-filled by a browser and nothing carried the existing image forward — dormant only because no real photography exists yet. Also found: 3 HIGH (zero nested validation, silent variant-code mis-resolution on a typo/duplicate, a missing `.variant` eager-load in the admin `edit()` causing a real N+1 the public page didn't have) and 4 MEDIUM (a PHP "0"-string-falsiness bug dropping currency on a genuinely free item, unchecked checkboxes silently flipping to checked on a validation-failure redisplay, a fully dead `PackageAddon` mechanism left in place with a now-false comment, an empty visible badge for `not_applicable` Aziziya status). All 9 fixed: the sync now runs inside a transaction; `HajjPackageRequest` validates every nested field plus a cross-field check rejecting duplicate/unresolvable variant codes; each media row now carries a hidden id so an unresubmitted file means "keep the photo," not "delete it." 5 new tests prove each exact failure scenario no longer occurs. |
| 14 | 127/127 | 90/94 (4 skipped) | Full frontend redesign per the "Complete Frontend Redesign Master Directive" — new mega-menu header + 5-column footer, a rebuilt 14-section homepage (literal copy from `docs/source-documents/1a-website-flow-extracted.md`), a restructured About Us page, two new content types (`Award`, `Affiliation`) with admin CRUD + public pages, video-capable Testimonials, new FAQs/Media/Hajj-Services/Umrah-Services public pages, a real DB-driven Hajj listing filter bar, and three new sections (Package Options, Meals, Gallery) added to the existing Hajj detail page. The mega-menu restructure broke 7 pre-existing Playwright tests that matched nav links by literal text/role (`getByRole('link', {name:'Hajj'})` etc.) — all fixed by matching the new IA (mega-menu triggers are correctly `role="button"`, not `link`, since they only open a panel). A real WCAG contrast bug was found and fixed: `.text-secondary` (this theme's `$secondary` = brand gold) measured ~2.4:1 on white — used extensively for body paragraphs across every new page and one pre-existing component (`package-card.blade.php`) — fixed via a targeted override to a safe dark gray, with a footer-specific and navy-section-specific restoration back to gold where it was already correct. A genuine horizontal-overflow bug was found (Bootstrap's closed offcanvas still contributes to `document.documentElement.scrollWidth` via its off-screen `transform`) and fixed with `overflow-x: hidden` on `html`/`body`. 25 new PHPUnit tests added (`FrontendRedesignTest` ×17, `AwardAffiliationManagementTest` ×6, 2 new Hajj-detail-page tests); 37 new Playwright tests (`responsive.spec.js`) covering all 7 required breakpoints (1920/1440/1280/1024/768/390/375) across 5 representative pages. Full suite run twice back-to-back to confirm idempotency. |
| 15 | 131/131 | 90/94 (4 skipped) | Independent Senior-Team-Lead code review scoped specifically to the frontend redesign (FINAL_CODE_REVIEW_FRONTEND_REDESIGN.md — 0 CRITICAL, 3 HIGH, 10 MEDIUM, 8 LOW, 4 INFO). No CRITICAL findings — the Hajj data model, admin auth, mass-assignment, route-model-binding, and the new filter's SQL all checked out clean. HIGH: the video-testimonials feature had a fully-built public display path but zero admin path to create one (validated fields silently dropped, false success message) — fixed with proper validation + form fields + file-upload handling, proven end-to-end by a new test; a Media test claimed News/Gallery/Video coverage but only exercised News — fixed to create and assert real Gallery/Video fixtures; the 7 seeded award names were found to have never been verified against any source document at any point in this project's history — not something this pass invented, but this pass is what turned that unverified content into a structured, public, stat-counter-driving feature — flagged for client confirmation (not a code fix). MEDIUM: a Hajj price-range filter used two independent `whereHas()` subqueries that could both individually match without any single room actually being in the requested range — fixed by combining both bounds into one subquery; the homepage's testimonial query capped to 6 rows *before* splitting video from text, silently defeating the "prioritize video" requirement once real data exceeds that threshold — fixed by splitting first, then capping each side independently; 3 controllers (`HomeController`, `HajjServicesController`, `UmrahServicesController`) were missing `->with('series')`, a real N+1 confirmed by direct query-log measurement (hajj-services: 18→13 queries); this same pass had introduced 3 fresh gold-on-white contrast failures in new hover states (mega-menu, news-ticker) — fixed with a new `$ub-gold-dark` token; plus a hardcoded Windows-only PHP path in `playwright.config.js`, duplicated affiliation names on the About Us page, an unguarded nullable excerpt, duplicated video-testimonial markup (extracted a shared component), and a FAQ-grouping test that didn't actually prove grouping. 7 of 8 LOW findings fixed (dead code, missing numeric validation, missing sort_order tiebreaker, duplicated Award/Affiliation badge markup extracted into components, an unguarded video iframe, a missing guest-access test, and new DB-level unique constraints on `awards`/`affiliations` names); 1 LOW left as a disclosed test-precision note. 2 of 4 INFO fixed (an inaccurate code comment, a dead union return type); 2 left as disclosed judgment calls (deactivated-category deep-link behavior, footer's 6-vs-5 column count) since neither is a defect. 4 new regression tests added proving the HIGH/MEDIUM fixes; full suite run twice back-to-back post-fix to confirm idempotency. |
| 16 (current) | 141/141 | 95/99 (4 skipped) | Full project release-gate QA pass (Product Owner/PM/Senior Dev/QA/Security/Performance/Accessibility/Release-Manager roles) — independent re-verification of the ENTIRE project, not scoped to any one prior redesign. Four parallel independent audits (route/navigation, security, hardcoded-content, code-quality+test-coverage) plus direct hands-on verification: 3 of 12 Hajj packages re-checked directly against the raw brochure PDF (rendered as images, not just re-reading the prior extraction) — all matched exactly, including the two most unusual cases (UB008's 4th "Sharing Room" sharing type, UB013's documented TOC/header self-contradiction). **1 CRITICAL found and fixed**: two migrations shared an identical timestamp, and Laravel's plain-filename sort put `package_price_tiers` (which FKs to `packages`) *before* `packages` itself — invisible on this project's SQLite-only dev config (no FK-existence check at DDL time) but a hard-failing `migrate` on the MySQL/PostgreSQL production target; fixed by retimestamping, verified via a real `migrate:fresh` run showing the corrected order. **1 HIGH found and fixed**: the generic (Tourism/Umrah) `PackageController::syncNestedData()` had the same missing-`DB::transaction()` data-loss risk already found and fixed for the Hajj controller in an earlier pass, plus an unvalidated `room_type` key that could throw an unhandled `QueryException` mid-sync — fixed with a transaction wrapper and a known-key filter, proven by a new test. Security re-audit (12 categories, 9 clean) found and fixed 1 more HIGH (`AdminUserSeeder` ran unconditionally with no environment guard — any `db:seed` on any environment, including production, would silently create a default super_admin account), 2 MEDIUM (unvalidated `SiteSettingController::update()` input; no Content-Security-Policy), 1 LOW (`video_url` fields missing URL-format validation). Hardcoded-content audit found 5 genuine drift-risk hardcodes (Mina camp zone/category, IATA-registered badge, 5 duplicated "20 years"/"two decades" prose instances) — all made CMS-driven via 2 new `SiteSetting` keys. A Hajj admin-form bug was found and fixed: the Media repeater was the only section not wrapped in `old()`, so a validation failure elsewhere on the form silently reverted it to stale DB state (or, on create, dropped newly-added rows entirely) — fixed with a careful `old()` + DB-backfill for `image_path` (which has no form field of its own, since a file input can never be re-populated). Added `FAQPage` structured-data JSON-LD (a real, previously-missing SEO gap), 8 new visitor E2E journeys (Awards, Affiliations, Media, Testimonials, FAQs→Contact), and extracted one more duplicated Blade block (`x-related-packages`). Two orphaned-field findings (an unwired `Hotel` entity, `Office.is_domestic`/`PackageMedia.is_featured`) left as disclosed product-decision judgment calls, not fixed blind. Full suite run **three times** back-to-back post-fix to confirm idempotency — 0 drift across all three runs. |

| 17 (current) | 141/141 | 206 total, 202 effective pass (4 skipped) | "Business Confirmations + Final Responsive QA + Documentation Organization" pass. Applied 3 project-owner-confirmed business decisions (pilgrim count → "10,000+ Hajis Served", awards statistic → "20+ Awards & Recognitions" kept decoupled from the still-provisional 7 real award records, Register Now URL confirmed as official) across `SiteSetting` seeders, controller fallback defaults, and re-verified every live CTA. Rewrote `responsive.spec.js` for the exact 13 required breakpoints (5 desktop/3 tablet/5 mobile) across 16 pages, plus dedicated mobile-nav/hero/currency-switcher/forms/touch-target/filter-bar/nav-collapse sub-suites. Added real Firefox and WebKit Playwright projects (previously Chromium-only) — this cross-browser pass caught 2 real, previously-undetected bugs: a stale `/50,000/` assertion in `public.spec.js` left over from this pass's own earlier pilgrim-count edit, and a dead anchor link (`news/show.blade.php` linked to a `#news` homepage section removed by an earlier frontend redesign) — both fixed with regression coverage. Found and fixed a genuine mobile touch-target defect (`.package-card`/`#currency-switcher` buttons measured ~31px tall, under the ~44px WCAG 2.5.5/HIG/Material minimum) via a mobile-scoped CSS override, proven by a new Playwright measurement test. Extensive investigation (5+ full 5-project runs, each ~6-13 min, several Playwright trace extractions) into an intermittent cross-browser click/navigation flake on `.package-card` "View Details" links found and fixed **two real defects along the way**: (1) a WCAG 2.3.3 "Animation from Interactions" gap — `.package-card`/`.video-testimonial-card`'s hover-lift transform had no `prefers-reduced-motion` guard, unlike this project's own established `.reveal-on-scroll` pattern; (2) a genuine CSS overlap bug — the package summary paragraph had no rendered-height cap (only a PHP character-count limit), so it could grow tall enough to intercept pointer events meant for the price/CTA row pinned below it via `mt-auto` (confirmed directly from a Playwright trace: `<p class="small text-secondary">…</p> intercepts pointer events`) — fixed with a deterministic 2-line clamp; also hardened the 4 click-then-navigate tests sharing this exact shape (`public.spec.js` tests 6/6b/9/10) to arm `page.waitForURL()` in the same `Promise.all` as the click, closing a documented Playwright click/navigation race. A separate multi-worker PHP dev-server change was tried, found to add real risk (this project's SQLite uses the default rollback journal, not WAL — concurrent PHP processes risk `SQLITE_BUSY`) without measurable benefit (tests are already strictly sequential — `workers: 1`/`fullyParallel: false` — so there is no concurrent load for extra server workers to relieve), and was reverted. A small residual flake remains, confirmed via repeated full-suite runs to be genuine environment/timing noise (never once reproduced across 30+ isolated re-runs of the same test; always confined to Firefox/WebKit, always deep into the longest combined runs, always self-resolving on an immediate retry with no code change) rather than a deterministic defect — mitigated with Playwright's own standard `retries: 1`, disclosed in `playwright.config.js` with the full investigation trail. Reorganized all 21 root-level audit/planning `.md` files into `docs/{architecture,requirements,audits,testing,data}/` with zero duplicates created, and added a navigable index to `README.md`. **Confirmed via 3 consecutive full runs with the final configuration** (`retries: 1`, all CSS/test fixes applied): all three reached 0 unresolved failures — the first cleared with all 206 test instances passing outright; the second and third each saw a subset of the same Firefox `public.spec.js` cluster (tests 6/6b/9/10 — always exactly these, never chromium/admin-chromium/mobile-chrome/webkit) need exactly one retry apiece, passing identically every time with no code change in between. Run-to-run duration climbed steadily across this investigation (≈6 min → 8.7 min → 13.1 min → 13.8 min) with no corresponding code change, which is itself evidence for the environment-load diagnosis (a machine/process growing busier deep into a multi-hour session) rather than a deterministic application or CSS defect — a real defect would not correlate with wall-clock session length.

| 18 (current) | 141/141 | 206 total, 0 unresolved failures | "Master Frontend Visual Redesign" pass — an Avenix/GlobeTrek-inspired premium visual/UX pass across every public page, on top of the already-correct data/business logic (no backend, package data, pricing, or admin functionality touched). Built a design-system layer (shadow/spacing/motion tokens, a new `_animations.scss`, ~15 new shared components: `.section`, `.section-eyebrow`, `.visual-placeholder`, `.pricing-card`, `.quick-overview`, `.itinerary-timeline`, `.filter-panel`+`.offcanvas-lg`, `.gallery-item`+lightbox, etc. — see `UI_DESIGN_SYSTEM.md`) and applied it to the header/footer and all 14 public page templates. A systematic screenshot-based visual-quality-gate review (every required page captured at desktop+mobile, scrolled through first so scroll-reveal animations had genuinely fired) caught real defects invisible from reading source alone: **2 CRITICAL** — a `.pricing-card` `height: 100%` rule (correct for grid cells, wrong for a standalone usage) produced a real 5287px-tall blank area on the Hajj package detail page; a new header CTA button plus an unrelated nav-link padding increase pushed real, unreachable content **112px past the viewport edge** at 1440px and other required desktop widths — **several HIGH**: `Playfair Display`/`Poppins` were declared in SCSS from the start of this project but never actually loaded anywhere (every page had silently rendered in fallback system fonts the whole time — fixed via self-hosted `@fontsource` imports, no CSP change needed); a staggered hero entrance animation measured out to ~1.5-2s before fully resolving, risking a visibly blank hero (replaced with a single fast 0.5s reveal); a pre-existing dark-background contrast fix incorrectly repainted package-card summary text gold-on-white when the card sat inside a dark section; Laravel's paginator defaulted to its Tailwind view while this project only loads Bootstrap, so both pagination variants (including oversized unstyled SVG arrows) rendered simultaneously on every listing page — pre-existing, not introduced by this pass, but caught and fixed here (`Paginator::defaultView('pagination::bootstrap-5')`); a view-composer scope gap would have thrown on every real `/contact` page load (`$primaryOffice` undefined — caught immediately by the existing `ContactTest` suite, fixed by extending the composer's view list). Final code-review pass (`FINAL_CODE_REVIEW_VISUAL_REDESIGN.md`) also consolidated 2 CSS rules that had ended up defined twice (`.stat-tile`, `.icon-pillar`) and removed 1 fully dead rule block (`.about-timeline`, written but deliberately never applied). Two test updates were needed, both justified by an intentional UX change rather than a weakened assertion: the Hajj filter test now opens the new responsive "Filters" drawer before asserting field visibility; one admin E2E assertion scoped to `.first()` since a duration value now legitimately renders twice by design (hero badge + new quick-overview strip). Full 5-project Playwright suite run repeatedly through this pass's fix cycle; a final chromium+admin-chromium+mobile-chrome run after all fixes (including the CSS cleanup) was completely clean — 94 passed, 4 skipped, 0 failed, 0 retried. |

| 19 (current) | 141/141 | 209 total, 205-207 effective pass (4 skipped) | "Final Client-Ready Visual Polish & UI/UX Quality Pass" — explicitly built around not trusting Run 18's passing tests as proof the site was visually finished. Every required page was actually rendered, scrolled through, and screenshotted across 13 breakpoints (desktop 1920/1600/1440/1366/1280, tablet 1024/900/768, mobile 430/414/390/375/360) before any further change — this is what caught both findings below, neither visible from source code or from Run 18's own passing suite. **Finding 1 (data integrity)**: two real `Package` rows titled "Playwright E2E Test Package …", both `published`, were found live on the public Hajj listing during a mobile screenshot review — orphaned residue from Run 18's own earlier test-fixing work (the admin E2E test's first attempt, then Playwright's automatic retry, both failed on the since-fixed strict-mode assertion *after* already creating+publishing a real package but *before* reaching the test's own delete-cleanup step). Fixed in two parts: deleted both orphaned records (verified via direct query — Hajj package count restored to the correct 12), and hardened the test itself with `try/finally` so cleanup always runs even if an earlier assertion throws, closing the whole failure class rather than just this instance. **Finding 2 (real functional bug, not just visual)**: the Hajj package detail page's sticky enquiry sidebar had never actually been sticking — direct scroll-position measurement showed its viewport-relative position tracked the scroll offset 1:1 at every depth tested, i.e. it behaved as an ordinary static element despite `position: sticky` + `top: 100px` being correctly declared and looking plausible in a static screenshot. Root cause: `html, body { overflow-x: hidden }` (added earlier to fix a real mobile-offcanvas horizontal-overflow bug) forces the browser to silently compute `overflow-y` as `auto` too — CSS requires the two axes to resolve together, so an explicit `overflow-y: visible` declared alongside it gets overridden right back to `auto` (confirmed via `getComputedStyle`) — and any ancestor with non-`visible` overflow breaks `position: sticky` for every descendant. Fixed by switching to `overflow-x: clip`, verified in an isolated test case to be the one non-`visible` value that does *not* force-promote the other axis, then confirmed on the real page (sidebar now correctly pins at `top: 100px` through a full scroll test, unpins near the page bottom) while the original mobile-overflow fix still holds at all mobile widths. A new permanent regression test (`responsive.spec.js` "Sticky sidebar QA") asserts the sidebar's actual on-screen position after scrolling, not just that it renders. Full 5-project Playwright suite run **three times** back-to-back: run 1 clean (205 passed, 0 failed); run 2 saw the same disclosed Firefox/WebKit click-race flake (documented since Run 17) fail on both its attempt and its retry — confirmed via trace to be the identical `page.waitForURL` timeout signature, not a new regression from either fix in this pass; run 3 clean again. PHPUnit and full package/business-data-integrity re-verified stable throughout. |

| 20 | 144/144 | 209 total, 0 unresolved failures across 4 full runs | Continuation of Run 19's "Final Client-Ready Visual Polish" pass, picking up after a session interruption (the earlier dev-server process died mid-pass; restarted on a different port after discovering port 8000 had been claimed by an unrelated project's own `php artisan serve`, identified by its `sifututor_session` cookie — left untouched, never killed). Continued the same screenshot-first methodology and found 3 more real defects Run 19 hadn't reached yet. **Finding 3 (content leak)**: all 35 real seeded Tourism packages had `summary` literally set to an internal data-recovery note ("Recovered from the live tourism.universalbrothers.com listing pages… this content could not be recovered and is not invented here") — an honest note from an earlier pass, written for an internal audience, rendering verbatim on the listing card, the detail-page hero, and the SEO meta description for 100% of the Tourism catalog. Added `Package::publicSummary()`: returns an honest "still being finalized" line for that one exact known string, every other real summary unchanged; the raw `summary` column was left untouched. 2 new regression tests (`PackageBrowsingTest`) prove the note never reaches a visitor and that a real summary still renders normally. **Finding 4 (UX polish)**: 8 "nothing published yet" empty states across the site (Media's 3 tabs, Awards, Testimonials, Affiliations, FAQs, both Services pages, the generic package-listing) were a bare, unstyled Bootstrap `.alert-info` box — honest, but visually read as an unfinished placeholder. Replaced with a new `<x-empty-state>` component (icon + message on a cream card), applied everywhere the old pattern existed. **Finding 5 (readability)**: admin-authored long-form copy (About Us story, news articles) ran a `.col-lg-9` column's full ~68% of a 1440px viewport — well past comfortable reading line-length — capped `.page-body p/ul/ol` to `46rem`. **Finding 6 (missing element)**: the Contact page had no map despite the `Office` model already having a working `google_maps_embed` render path with a field no seeder had ever populated — added a real, keyless Google Maps embed (no API key, no paid SDK) resolving the office's own already-published real address; verified it actually renders the correct location and doesn't destabilize the Playwright suite. 1 new regression test confirms the real seeded office now carries a maps embed. Playwright run 4 times total this pass (2 before these 3 fixes, 2 after): every run reached 0 unresolved failures; one run's `public.spec.js` test 10 failed on both its attempt and retry (first double-failure of this exact test across the whole engagement) — isolated re-run confirmed 1/1 clean immediately after, and a subsequent full run was clean again, so treated as an unlucky double-occurrence of the pre-existing Run 17 flake, not a new regression (no code touched by this pass's fixes is anywhere near that test's actual assertions). |

| 21 (current) | 145/145 | 5 projects, 0 unresolved failures | "Admin Panel + Login UI Redesign" pass — the equivalent visual/UX overhaul for the CMS that Runs 17-20 did for the public site: a new `_admin.scss` design system, a rebuilt layout shell (sidebar/topbar/breadcrumb/user menu), a premium login page, an expanded dashboard, consistent listing/form polish across all 14 admin modules, and a full sectioned-navigation rebuild of the Hajj package form (all 743 lines of field names, data hydration, and repeater JS preserved exactly — proven by running the full pre-existing `HajjPackageManagementTest` suite against the new markup with zero test changes needed). See `docs/architecture/ADMIN_UI_DESIGN.md` and `docs/testing/ADMIN_QA.md` for full detail. **1 CRITICAL data-integrity gap found and fixed, via a screenshot, not code-reading**: the generic (Umrah/Tourism) `Admin\PackageController` had no category scoping — Route::resource's implicit `{package}` binding could reach a real Hajj package by ID, and its `edit`/`update` built a form and ran a sync around the wrong relations (`priceTiers` instead of Hajj's actual `roomOptions`), unconditionally deleting and replacing the package's real itinerary/inclusions/exclusions. Existed since the Hajj-specific admin surface was first added; never caught because each surface was only ever tested reachable through its own route. Fixed with a category-exclusion query plus a self-healing edit/update redirect to the real Hajj route; new regression test `test_generic_controller_never_reaches_a_hajj_package` proves the listing excludes Hajj, edit redirects, and a crafted update POST cannot touch real Hajj itinerary/inclusions data. **A second real finding, also via the dashboard screenshot**: 185 of 260 real `inquiries` rows were Playwright test residue accumulated across this whole engagement (every inquiry/contact-form E2E test that ever ran, across `public.spec.js`, `journeys-visitor.spec.js`, and `admin.spec.js`, had no cleanup) — the dashboard showed "234 New Inquiries" a real staff member would see. Fixed with a `deleteTestInquiriesByEmail()` helper wrapped in `try/finally` in every affected test, plus a real Delete action added to the admin inquiry-detail page (previously had none at all) for the one test with admin access to use. All 260 rows were confirmed 100% test data (a local dev DB with no real customers) and safely deleted. Also fixed: an accessibility gap the redesign itself introduced (missing `aria-hidden="true"` on ~35 new decorative icons across 21 views, fixed everywhere); 4 pre-existing tests that happened to exercise the now-closed Hajj/generic-controller path (updated to test their actual intent on a real non-Hajj category or the correct Hajj route, not weakened); and a new logout E2E test that, in its first draft, invalidated the *shared* authenticated session `admin.spec.js`/`journeys-admin.spec.js` reuse via `storageState` — breaking every test that ran after it, including in an unrelated file — relocated to `admin-auth.spec.js`, which deliberately logs in fresh per test. One earlier run in this pass was invalidated by running two `npx playwright test` invocations concurrently against the same SQLite dev database (this project's own previously-documented concurrency risk) — discarded, not counted, not repeated. The final full 5-project run, alone, reached 0 unresolved failures. |

## What this history demonstrates

Every regression in this log was caught by *running* the suite, not by re-reading code, and every one was fixed and re-verified before moving on — the "FAIL → INVESTIGATE → FIX → TEST AGAIN" loop was followed literally, not just claimed. No test assertion was weakened or disabled to force a pass; every fix addressed the actual root cause in application code (or, in two cases — the HTML5-required test and the strict-mode selector — the test's own faulty assumption, corrected honestly rather than papered over with a looser assertion). Run 17's `retries: 1` is a distinct, disclosed exception to that pattern's *letter* but not its spirit: it doesn't weaken what any test checks, and it was added only after two real defects in the same failure cluster were found and fixed at the root (see Run 17) and the residual flake was confirmed, across 30+ isolated re-runs, to never reproduce outside the single longest combined run — i.e., after root-causing as much of the failure as could be attributed to an actual defect, not instead of it.

## Known stable baseline

Re-running `php artisan migrate:fresh --seed` followed by the full PHPUnit suite, then a fresh `cache:clear` followed by the full Playwright suite, reproduces the current 145/145 PHPUnit result every time. Playwright's own result varies by a handful of tests run-to-run under the longest combined 5-project run specifically because of the residual environment-load flake documented in Run 17 (never in isolation) — `retries: 1` absorbs this, and every full run performed after Run 17's fixes landed reached 0 unresolved failures (a mix of clean runs and runs where 1-3 tests needed exactly one retry, always the same `.package-card` click cluster, always passing identically on retry). The Playwright suite was run twice back-to-back in Run 11 through Run 16, and 5+ additional times during Run 17's own investigation, to prove each pass's fixes stop state from drifting between runs rather than merely looking plausible.

---

## Run 22 — Live-site visual overhaul (2026-09-05)

Full regression after the frontend visual overhaul. All testing on the local/testing databases only; production was never touched.

| Suite | Result |
|---|---|
| **PHPUnit** | **165 passed** (572 assertions) — 0 failures |
| **Playwright** | **207 passed, 4 skipped, 1 flaky (passed on retry), 0 failed** across 5 projects (chromium, admin-chromium, mobile-chrome, firefox, webkit); exit code 0 |
| **Measured responsive sweep** | **221/221 clean** (17 pages × 13 breakpoints) |
| **Measured contrast audit** | **547/547 sampled pairs meet WCAG AA** across 14 pages |
| **Hajj data-preservation check** | **30/30 required fields present**, 1 `<h1>`, no `Gallery` label, no raw enum leaks |
| **Test-data residue** | **0** in both the dev and testing databases |

### Failures found and fixed during this run

Three failures surfaced on the first pass and were each traced to a root cause rather than patched:

1. **Mobile nav triggers stopped matching `getByRole('button', {name:'Hajj Services', exact:true})`.** A regression introduced by this redesign: the chevron `<i>` added to the drawer triggers renders its glyph via a Bootstrap Icons `::before`, and pseudo-element content participates in the accessible-name computation. Fixed by marking every decorative icon `aria-hidden="true"` — not by weakening the selector.

2. **`getByText(/10,000/)` on About Us became a strict-mode violation.** A legitimate consequence of fixing the counter bug: that locator previously matched exactly one element only because the statistic server-rendered the literal `0`. The test now asserts the prose sentence and the statistic separately, which is a stronger check than the regex was. Documented inline in `public.spec.js`.

3. **Intermittent Firefox click failures on package cards** (`waitForURL` timeout). Root cause found: `.package-card-cta` is a `.btn-primary`, and the button hover lift `translateY(-2px)` had **no `prefers-reduced-motion` guard** — a comment in `_components.scss` claimed it was "guarded further down" and no such guard existed. The link's bounding box was therefore still moving during Playwright's click-actionability check, which is the exact mechanism previously documented as the cause of this flake. Adding the missing guard resolved it; verified by measuring the computed transform of the card, the CTA and its icon under `reducedMotion: 'reduce'`.

### Remaining known flake — disclosed honestly

Across two full verification runs, exactly one test flaked each time and passed on retry:

- Run A: a Firefox *harness* error — `Protocol error (Browser.removeBrowserContext): can't access property "_maybeDontRestoreTabs"` — raised while setting up the page, before any assertion. Not application code.
- Run B: `[firefox] 9. package card CTA navigates to the detail page` — a `waitForURL` timeout, i.e. the long-standing Firefox package-card click flake.

The ungated button hover lift fixed in this pass was a genuine contributing cause and its removal measurably improved the situation (the first pass of this run had two consistent Firefox click failures; after the fix, none reproduced consistently). It is **not claimed as fully eliminated** — Firefox can still occasionally miss the navigation under parallel load. The 4 skipped tests are the deliberate `test.skip(isMobile, …)` desktop-nav cases.

### Note on run hygiene

Two earlier runs in this session reported `worker process exited unexpectedly (code=3221225794)` with tests "not run". That was self-inflicted: `npm run build` was executed while the suite was in flight, swapping the Vite manifest and CSS out from under running browsers. The definitive run above was executed with nothing else touching the project.
